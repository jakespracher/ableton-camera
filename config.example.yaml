osc:
  send_host: 127.0.0.1
  send_port: 11000
  listen_host: 127.0.0.1
  listen_port: 11001

obs:
  host: 127.0.0.1
  port: 4455
  password: ""

paths:
  staging_dir: ~/Movies/OBS-Staging

naming:
  track_merge: "_"
  # Subfolder under the output directory: output_dir/{project}/track_timestamp.ext
  # Defaults to today's date (YYYY-MM-DD) when empty. Override: --project "My Album"
  project: ""

sync:
  # Reminder value from docs/SYNC.md; the bridge writes it to last_take.json for Extensions.
  obs_source_sync_offset_ms: 0
